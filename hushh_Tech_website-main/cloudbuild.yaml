# =============================================================================
# Google Cloud Build — CI/CD Pipeline for Hushh Tech Website
# Trigger: Push to main branch → Build Docker image → Deploy to Cloud Run
#
# Setup:
#   1. gcloud builds triggers create github \
#        --repo-name=hushh_Tech_website \
#        --repo-owner=hushh-labs \
#        --branch-pattern='^main$' \
#        --build-config=cloudbuild.yaml \
#        --substitutions=_REGION=us-central1,_SERVICE_NAME=hushh-tech-website
#
#   2. Set substitution variables in the trigger for all VITE_* and server env vars
# =============================================================================

steps:
  # Step 1: Build Docker image with build args for VITE_* env vars
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'Dockerfile.gcp'
      - '-t'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest'
      - '--build-arg'
      - 'VITE_SUPABASE_URL=${_VITE_SUPABASE_URL}'
      - '--build-arg'
      - 'VITE_SUPABASE_ANON_KEY=${_VITE_SUPABASE_ANON_KEY}'
      - '--build-arg'
      - 'VITE_SUPABASE_REDIRECT_URL=${_VITE_SUPABASE_REDIRECT_URL}'
      - '--build-arg'
      - 'VITE_KYC_ENV=${_VITE_KYC_ENV}'
      - '--build-arg'
      - 'VITE_KYC_DEMO_MODE=${_VITE_KYC_DEMO_MODE}'
      - '--build-arg'
      - 'VITE_KYC_API_BASE=${_VITE_KYC_API_BASE}'
      - '--build-arg'
      - 'VITE_GEMINI_API_KEY=${_VITE_GEMINI_API_KEY}'
      - '--build-arg'
      - 'VITE_FIREBASE_API_KEY=${_VITE_FIREBASE_API_KEY}'
      - '--build-arg'
      - 'VITE_FIREBASE_AUTH_DOMAIN=${_VITE_FIREBASE_AUTH_DOMAIN}'
      - '--build-arg'
      - 'VITE_FIREBASE_PROJECT_ID=${_VITE_FIREBASE_PROJECT_ID}'
      - '--build-arg'
      - 'VITE_FINNHUB_API_KEY=${_VITE_FINNHUB_API_KEY}'
      - '--build-arg'
      - 'VITE_GEMINI_API_KEY_FALLBACK_1=${_VITE_GEMINI_API_KEY_FALLBACK_1}'
      - '--build-arg'
      - 'VITE_GEMINI_API_KEY_FALLBACK_2=${_VITE_GEMINI_API_KEY_FALLBACK_2}'
      - '--build-arg'
      - 'VITE_GEMINI_API_KEY_FALLBACK_3=${_VITE_GEMINI_API_KEY_FALLBACK_3}'
      - '--build-arg'
      - 'VITE_FIREBASE_STORAGE_BUCKET=${_VITE_FIREBASE_STORAGE_BUCKET}'
      - '--build-arg'
      - 'VITE_FIREBASE_MESSAGING_SENDER_ID=${_VITE_FIREBASE_MESSAGING_SENDER_ID}'
      - '--build-arg'
      - 'VITE_FIREBASE_APP_ID=${_VITE_FIREBASE_APP_ID}'
      - '--build-arg'
      - 'VITE_N8N_WEBHOOK_URL=${_VITE_N8N_WEBHOOK_URL}'
      - '--build-arg'
      - 'VITE_OPENAI_API_KEY=${_VITE_OPENAI_API_KEY}'
      - '--build-arg'
      - 'VITE_VOICE_AGENT_URL=${_VITE_VOICE_AGENT_URL}'
      - '.'

  # Step 2: Push Docker image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:$COMMIT_SHA']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest']

  # Step 3: Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - '${_SERVICE_NAME}'
      - '--image'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:$COMMIT_SHA'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--concurrency'
      - '250'
      - '--timeout'
      - '60s'
      - '--set-env-vars'
      - 'NODE_ENV=production,VITE_SUPABASE_URL=${_VITE_SUPABASE_URL},VITE_SUPABASE_ANON_KEY=${_VITE_SUPABASE_ANON_KEY},OPENAI_API_KEY=${_OPENAI_API_KEY},GEMINI_API_KEY=${_GEMINI_API_KEY},VITE_FIREBASE_PROJECT_ID=${_VITE_FIREBASE_PROJECT_ID}'

substitutions:
  _SERVICE_NAME: hushh-tech-website
  _REGION: us-central1
  _VITE_KYC_ENV: production
  _VITE_KYC_DEMO_MODE: 'false'

timeout: '1200s'

images:
  - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest'